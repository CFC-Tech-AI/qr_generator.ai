{
    "builds": [{
        "src": "qr_generator/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "10mb", "runtime": "python3.8" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "qr_generator/wsgi.py"
        }
    ]
}